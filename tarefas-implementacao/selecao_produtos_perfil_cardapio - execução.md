# Execução: Seleção de Produtos no Perfil de Cardápio

## Status Atual
- **Fase Atual**: Implementação do frontend no Admin (`admin.html` e `admin.js`) e integração com a tabela `perfil_cardapio_produtos`.

## Checklist
- [x] 1. Alterar `admin.html` para adicionar abas "Categorias" e "Produtos" no modal de Perfil.
- [x] 2. Modificar `admin.js` (`carregarPerfisCardapio`, `abrirModalPerfilCardapio` e `salvarPerfilCardapio`) para carregar, exibir e salvar os produtos individuais vinculados.
- [x] 3. Atualizar o filtro de cardápio no `index.js` para unir as categorias permitidas e os produtos permitidos individuais.
- [x] 4. Testes e validação.

## Log de Modificações
*Nenhuma modificação realizada no código ainda.*
